# Keep shell scripts LF so they run inside Linux containers even when the repo
# is checked out on Windows.
*.sh text eol=lf
entrypoint.sh text eol=lf

# Normalize line endings for text; let Git decide otherwise.
* text=auto
